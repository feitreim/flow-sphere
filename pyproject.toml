[project]
name = "sphere"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.5.0",
    "einops>=0.8.2",
    "torch>=2.10.0",
    "torchmetrics[image]",
    "torchvision>=0.25.0",
    "tqdm>=4.67.3",
    "wandb>=0.19, <0.25",
]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "TCH",   # flake8-type-checking
    "RUF",   # ruff-specific rules
]
ignore = ["F722"]  # jaxtyping annotations use syntax pyflakes doesn't understand

[tool.ruff.lint.isort]
known-first-party = ["tome"]

[tool.pyright]
pythonVersion = "3.14"
typeCheckingMode = "standard"
venvPath = "."
venv = ".venv"
